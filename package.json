{
  "name": "hyundai-lowpoly-toon",
  "version": "1.0.0",
  "description": "Low poly toon-shaded 3D scene viewer",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.21"
  }
}
